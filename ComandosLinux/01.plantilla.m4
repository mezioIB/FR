# Ejemplo donde se realiza una operación simple y se imprime en pantalla un mensaje
#
# Se ejecuta con el comando:
# feenox 01.duplica_{factor}.fee
# 

entrada = {factor}
salida = 2*entrada
PRINT SEP " " TEXT "2 *" entrada "=" salida
